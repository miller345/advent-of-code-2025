# Advent of Code 2025

to run (with .net sdk installed or inside the dev container):

```sh
dotnet fsi day1.fsx
```

or with docker:

```sh
docker run --rm -v "$PWD":/src -w /src mcr.microsoft.com/dotnet/sdk:10.0 dotnet fsi day1.fsx
```
